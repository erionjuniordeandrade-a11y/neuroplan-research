# Dev/test dependencies for the standalone (non-Slicer) safety modules.
# The Slicer extension itself runs inside 3D Slicer's bundled Python.
pydicom>=2.4
numpy>=1.24
pytest>=7.4
# UX layer dashboard (only needed to RUN app.py; the core + tests do not import it).
streamlit>=1.30
